#include<bits/stdc++.h>
using namespace std;

struct ListNode {
    int val;//节点值
    ListNode* next;//指向下一个节点的指针
    ListNode(int x) :val(x), next(nullptr) {}//构造函数
};


int main() {
    //初始化链表 1-
    ListNode  
    return 0;
}