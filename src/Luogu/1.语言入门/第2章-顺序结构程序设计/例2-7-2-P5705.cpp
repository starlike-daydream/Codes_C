#include <bits/stdc++.h>
using namespace std;

int main()
{
    char a, b, c, dot, d;
    cin >> a >> b >> c >> dot >> d; // 字符容量有限，输入不需要空格/换行
    cout << d << dot << c << b << a << endl;
    return 0;
}