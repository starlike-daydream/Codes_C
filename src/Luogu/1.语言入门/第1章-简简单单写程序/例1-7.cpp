#include <iostream>
#include <cmath>
using namespace std;

int main() {
    cout << sqrt(pow(6,2) + pow(9,2));
    return 0;
}

//cmath头文件含sqrt计算平方根，ceil向上取整，floor向下取整，pow计算幂，fabs计算绝对值，返回值和参数都是浮点数double